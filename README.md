# Fokins Custom Woodwork — website (fokinsllc.com)

Recovered source of the live site (v41), pulled from production on 2026-07-31 after the
original source was lost from git. This is now the single source of truth.

Static HTML/CSS/JS. Deployed on Netlify (project: stirring-mooncake-ba2af5).

## Deploy
Connect this repo to the Netlify project → every push to `main` auto-deploys.
Netlify serves clean URLs (`/services` → `services.html`) automatically.
